# Development package verification — 2026-09-13

## September 14: HTTPS installer candidate

[Run 34834821106](https://github.com/EntreePOS/entree-print/actions/runs/34834821106) passed from `911ee37c135d270023ee86fcc74b45c4dcc20717`. Downloaded TRX confirms **386 .NET tests executed and passed**, with no failures or skips. Workflow logs confirm **98 core SDK tests, nine native SQLite tests and nine isolated installer checks**. All **64 hosted installer lifecycle checkpoints** passed, including permissions over **739 installed entries**. Five native logs confirm successful install/update/uninstall/reinstall/final uninstall, without reboot. The [recorded result](installer/evidence/windows-ci-34834821106.json) retains the exact source, checks and digest.

The unsigned EXE is **80,851,005 bytes**, SHA-256 `17f801364d9eac6f11df7e83f714c6970815d774cbfaa404de5e0302ffb6ef46`. Package manifest SHA-256 is `a551f00c972fa3d498172e3159a14a399d0df922ab23957e98d7c677b1098a82`. The downloaded EXE hash matches build metadata, SHA256SUMS and lifecycle evidence. [Artifact 10343960615](https://github.com/EntreePOS/entree-print/actions/runs/34834821106/artifacts/10343960615) retains the candidate and evidence for 14 days; local files are under `%TEMP%\EntreeInstallerCI-34834821106`.

This candidate includes optional certificate-store HTTPS, protocol-aware discovery/recovery, separated HTTP/HTTPS monitors and the localhost playground fallback. The first HTTPS run (34834324901) passed all TLS cases but hit an existing command-discovery fixture's 20-second deadline; this candidate uses the application's bounded 90-second deadline and retains the dedicated timeout test. The public guide/playground deployed successfully from `a0eb732` and the playground was checked after reloading in the browser.

TLS tests use real Kestrel over memory transports and verify successful encryption plus wrong-name/untrusted-issuer rejection. Hosted installer checks keep printing inactive. Neither establishes installed service private-key access, interactive UAC/logon, client certificate trust, live LAN/physical recovery or automatic printing failover. No GitHub Release was created. Subsequent edits record evidence and current host status only.

## September 14: ordered actions and Chromium startup candidate

[Run 34831638120](https://github.com/EntreePOS/entree-print/actions/runs/34831638120) passed from `02110129a19bd07b4ef97ee5de43abeb4d601542`. Downloaded TRX confirms **374 .NET tests executed and passed**, with no failures or skips. Workflow logs confirm **95 core SDK and nine native SQLite tests**, plus nine isolated installer checks. All **64 hosted installer lifecycle checkpoints** passed, including permissions over **739 installed entries**. All five native logs report successful installation/update/uninstall/reinstall/final uninstall without reboot. The [recorded result](installer/evidence/windows-ci-34831638120.json) retains the source, checks and installer digest.

The unsigned setup EXE is **80,858,574 bytes**, SHA-256 `f1ff6543401ff8134ad57644f60f94eaf54d6b68f4487f6fdd7cf39378b615ff`. Package manifest SHA-256 is `9b228697b9c3c90e3c2923c8a2842514f21b89baed81f1679811833b2ed301c0`. The downloaded EXE hash matches both build metadata and lifecycle evidence. [Artifact 10342652173](https://github.com/EntreePOS/entree-print/actions/runs/34831638120/artifacts/10342652173) retains the candidate and evidence for 14 days; local evidence is under `%TEMP%\EntreeInstallerCI-34831638120`.

This candidate includes ordered receipt/cut/beep phase storage and the Chromium startup-file sharing fix. The earlier failed runs did not build installers: the first exposed a test deadline race; the second captured the renderer's actual sharing violation. Those cases pass in this candidate. Silent hosted verification left printing inactive; it does not prove interactive UAC/logon, service-account activation, no-runtime client deployment or physical/network recovery. No GitHub Release was created. Subsequent documentation edits update stale feature descriptions and token setup only.

## September 14: resumable status-stream candidate

[Run 34827971685](https://github.com/EntreePOS/entree-print/actions/runs/34827971685) passed from `d2fd553335b71bb613098d20bd888e0d9ba5c904`. Downloaded TRX and workflow logs confirm **350 .NET tests, 93 core SDK tests and nine native SQLite tests**, all passing. The installer passed **64 lifecycle checkpoints**, including permissions across **739 installed entries**. All five native logs end in successful install/update/uninstall/reinstall/final uninstall, with no reboot. [Recorded result](installer/evidence/windows-ci-34827971685.json) retains the source, installer digest and checks.

The unsigned installer is **80,840,863 bytes**, SHA-256 `e458a8d906c5a97d62c62f15f95c0f33a327babdfc62a6dffcd7ebacef64c21b`; build metadata records package manifest SHA-256 `19e542cc15821e43e52038d440a830f0fc39890680b86527e8901588c9437cd4`. The downloaded EXE hash matches the lifecycle result and build metadata. [Artifact 10341461250](https://github.com/EntreePOS/entree-print/actions/runs/34827971685/artifacts/10341461250) contains the candidate/evidence with 14-day retention. Local evidence: `%TEMP%\EntreeInstallerCI-34827971685`.

This packages durable event history, authenticated SSE, printer checkpoints/removals and SDK resumption/reconciliation. The actual SDK/TestServer replay scenario uses zero print/render submissions across ledger reopen. Installer verification keeps printing inactive on a hosted administrator VM; it does not establish interactive UAC/logon, service activation, live LAN or physical fault recovery. No GitHub Release was created. The subsequent `439c1b9` change only shortens the public site's example lines; it changes no packaged SDK or application file. Published guide and playground were checked in the browser after deployment.

## September 14: native SQLite outbox candidate

[Run 34823530375](https://github.com/EntreePOS/entree-print/actions/runs/34823530375) passed from `bc96eef5a8cd208df178a4d67a0a0b27d1436ae7`. Downloaded TRX/logs confirm **320 .NET tests, 81 core SDK tests and nine native SQLite tests** passed. All **64 installer lifecycle checkpoints** passed, including permissions across **738 installed entries** with the additional SDK adapter. Five native logs report successful install/update/uninstall/reinstall/final uninstall without reboot. [Recorded result](installer/evidence/windows-ci-34823530375.json) preserves the exact source, installer digest and checkpoint list.

The unsigned installer is **80,841,764 bytes**, SHA-256 `2961c27582e7287cfac2e279471b742193d7cf9d21a5431eb7678ce50e7b5c94`; package manifest SHA-256 `10ca4d64eb64b349971732ff92259122e194ea964a2777a62bb08e5fc4bf336d`. The downloaded EXE digest independently matches metadata and the result. [Artifact 10338708667](https://github.com/EntreePOS/entree-print/actions/runs/34823530375/artifacts/10338708667) contains the candidate and evidence with 14-day retention; local evidence is under `%TEMP%\EntreeInstallerCI-34823530375`.

SQLite checks include a real Node process exit while holding a queue lock, independent queue access and previously committed wire recovery. The seventh SDK/TestServer scenario uses the production adapter with Chromium and service-ledger reopen. These do not prove machine power-loss behavior, network filesystems, cross-device ownership or live Windows printer recovery. Installer scope remains silent hosted administrator operation with printing inactive. No GitHub Release was created.

## September 14: bounded settings-request candidate

[Run 34822273890](https://github.com/EntreePOS/entree-print/actions/runs/34822273890) passed from source `297a4645b5018f1469886bb30c983ccbbd8dec4b`, including the settings-request correction and configured initial backup connections. The downloaded TRX confirms **319 executed/passed, zero failed or skipped**. The installer completed all **64 lifecycle checkpoints**; its five native logs report successful install/update/uninstall/reinstall/final uninstall without reboot. The fixed installed settings helper saved successfully. [Recorded result](installer/evidence/windows-ci-34822273890.json) retains the exact checkpoint list and source identity.

Unsigned installer: **80,820,456 bytes**, SHA-256 `d03db4b7ce4f2a2273cbc7737dfb87561fe57adb1869d27e0fb4cf9e7bf958f1`. Package manifest SHA-256: `01bf63cfde234fe69c3bae5cbc6cdb83e3308552c9c815a6b71eb8f48f85d7af`. The downloaded EXE hash independently matches both build metadata and the lifecycle result. [Artifact 10339345988](https://github.com/EntreePOS/entree-print/actions/runs/34822273890/artifacts/10339345988) contains the candidate and evidence (14-day retention); local evidence is under `%TEMP%\EntreeInstallerCI-34822273890`.

The scope remains silent installation/storage mechanics on a hosted administrative Windows VM. Printing remained inactive, and retained receipt files were synthetic. This does not close interactive UAC/logon, service-account activation, no-installed-runtime, live network or physical printer recovery checks. No GitHub Release was created.

## September 14: real hosted Windows installer lifecycle

[Workflow run 34819567689](https://github.com/EntreePOS/entree-print/actions/runs/34819567689) passed from source commit `bb1432be8ed81fd06567da864461b12564ea7674` on a fresh `windows-2025` hosted VM (installer log Windows build 10.0.26100). All **308 .NET tests**, the nine isolated lifecycle cases and **64 recorded installer checkpoints** passed. The checkpoints include repeated phase checks; they are not 64 independent test scenarios. The exact [result JSON](installer/evidence/windows-ci-34819567689.json) is retained in source.

The real setup performed silent installation, same-version update, uninstall, reinstall and final uninstall. Every setup/uninstall process returned zero and its native log reports success without reboot. The verifier checked 737 installed file/directory entries, including bundled runtime dependencies, for trusted ownership, write protection and absence of links. It checked private spool permissions, the fixed installed settings-save helper, and the fixed service-action helper's absent-service uninstall path. Silent setup left printing and the interactive tray inactive. Start menu/desktop shortcuts and the matching loaded-user startup entry were removed by uninstall. Configuration and two synthetic receipt fixtures retained identical SHA-256 digests through update, uninstall and reinstall.

The first run, [34819173670](https://github.com/EntreePOS/entree-print/actions/runs/34819173670), stopped before building an installer: two renderer fixtures encountered late Chromium file locks and two shell fixtures exceeded their artificial 20-second deadline. Rendering now retries deletion of its own temporary browser profile for up to 1.9 seconds after killing/waiting for the root browser. The six dot-alignment cases assert profile removal. Shell fixtures now use the production 90-second deadline; the explicit one-second timeout test remains. Both the local suite and second hosted suite passed after these changes.

The tested unsigned candidate is **80,830,917 bytes**, SHA-256 `ba24799fd0de6dd1d101a736935cb3166e149da6c19247a69ee38627089e83bb`; package manifest SHA-256 `5956e2ad1696b440e0a8c5f7152cebe53330267fae75d8c166b54afa6ecd1116`. Compiler: Inno Setup 7.1.0, verified pinned download and valid signature. [Workflow artifact 10337742507](https://github.com/EntreePOS/entree-print/actions/runs/34819567689/artifacts/10337742507) contains the installer, hashes, metadata, TRX and five native installer logs and is retained for 14 days. Downloaded evidence is under `%TEMP%\EntreeInstallerCI-34819567689`; its installer digest was independently recomputed and the TRX confirms 308 executed/passed, zero failed/skipped/aborted.

Limits: [hosted Windows runners](https://docs.github.com/en/actions/reference/runners/github-hosted-runners#administrative-privileges) run as administrators with UAC disabled and include development runtimes. This validates installer/storage mechanics on that VM, not UAC prompts, alternate-admin identity, Windows client installation without .NET, fresh-user tray visibility, live service startup, accepted-job recovery or printer/network failures. The waiting/uncertain files were byte-retention fixtures, not accepted ledger jobs. The developer host's prototype service was not replaced. No GitHub Release was created.

## September 14: fixed elevated service commands

The tray now requests `--service-action install|start|stop|restart|uninstall` on its own trusted executable. The elevated process resolves only its adjacent installed service and the protected default configuration. It constructs commands internally, with no user-temporary script or error-output file. Service ownership must exactly match the quoted executable before any service/firewall mutation. System PowerShell runs without profiles, with system-only command/module search, UTF-16LE encoded command text, UTF-8 output and a 90-second deadline. Timeout reports an uncertain service state instead of inviting automatic retry.

All **308 .NET tests** pass (`%TEMP%\EntreeServiceCommandTests.log`). Service fixtures execute the production shell wrapper with Windows mutations replaced: all five owned actions succeed; all five reject foreign, unquoted and extra-argument commands; failed ownership lookup stops all work; missing service rules, native/firewall failures, Chinese/literal paths, module search and timeout behavior are covered. Read-only command discovery also confirms that the required real Windows cmdlets and system sc.exe resolve with the restricted search path. An initial fixture caught PowerShell expanding its module search during startup; the final wrapper resets the module path before executing commands. These checks do not perform UAC, install a service or change firewall rules.

A read-only Windows inventory still reports the existing prototype running as PID 6252 at the original workspace `dist\EntreePrintPlugin\service\EntreePrintPlugin.exe` path. No live upgrade or service operation was attempted. Installed helper startup, clean-host permissions, alternate administrator approval and service-account deployment remain required.

Fresh candidate built from source commit `6c1ae1f7380ec302c71308dbf69f3cbe3bea3b8e`: `dist/EntreePrint-0.0.1-beta-win-x64-service-command`, with 701 verified files, .NET 10.0.12 and eight matching icon frames in each executable. The tray's product version records that source commit. Installer: `dist/installer-service-command/EntreePrint-0.0.1-beta-win-x64-setup.exe`, **80,826,308 bytes**, SHA-256 `c1e676b2be7b21d2e48f766b3166142b3d05631aa68cf3f1de88acd1f0dbf833`. Input manifest SHA-256: `c0a48bdf6d330779abafdc8998360a1eab630f5edc384ba5586678b3eba2d057`. Inno Setup 7.1.0 compiled successfully; the installer remains unsigned and was not executed or uploaded. Logs: `%TEMP%\EntreeServiceCommandPublish.log` and `%TEMP%\EntreeServiceCommandInstaller.log`.

PowerShell argument behavior follows [Microsoft's Windows PowerShell command-line reference](https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.core/about/about_powershell_exe?view=powershell-5.1). Encoding is for correct argument transport, not authentication or concealment.

## September 14: standalone repository and public usage guide

Published [EntreePOS/entree-print](https://github.com/EntreePOS/entree-print) with initial source commit `b53158d4ac3df339a9f6bdfbc02e3ec4633d2c7d`: 185 source, test and documentation files, excluding generated binaries, local configuration, receipt ledgers and client-specific source-review notes. The root README links the standalone guide. This working checkout now tracks the new origin; the original prototype commit remains on `codex/prototype-history` and its remote is named `prototype`. Local working files were preserved.

The [usage site](https://entreepos.github.io/entree-print/) deployed through [GitHub Actions run 34813602033](https://github.com/EntreePOS/entree-print/actions/runs/34813602033). The public page was opened and verified with all headings and images loaded. The guide includes nine generated/copyable snippets, 27 unique IDs with valid anchor targets, setup/tray guidance, receipt preview, codes, job semantics, discovery, retry, outbox and device helpers. The preview SVG is extracted unchanged from the saved synthetic cashier artifact; responsive styling scales the image without changing its drawing.

Desktop light/dark and 320/375 px mobile layouts were visually inspected. A narrow-screen overflow and preview clipping issue was corrected. Topic search, its empty state and copy feedback were exercised. The final 320 px check reports a 305 px page width inside the viewport, with the full receipt visible. The public page also reports no horizontal overflow and all images loaded. Viewport overrides were reset after testing.

A clean archive of the published source passes **277 .NET tests**, plus site snippet/asset verification. Log: `%TEMP%\EntreeStandalonePublishedTests.log`; source location is recorded in `%TEMP%\EntreeStandaloneCheckPath.txt`. Existing SDK/package checks remain 65 and 21 respectively; this turn changed documentation, source publication and site files. No installer or GitHub release asset was published. Installation, protected transport and hardware failure gates remain open.

## September 14: standalone client outbox and product README

The user clarified that this is a standalone product, with Entree POS as one client, and requested `EntreePOS/entree-print`, a README, usage site, installer and a GitHub release asset once mature. A root README now introduces the product and current API. The existing remote still points to printConnect; the requested destination was not available through the signed-in account's initial `gh repo view`. No repository, site or GitHub release has been created or pushed by this check.

The SDK now provides an optional durable client outbox, using IndexedDB/Web Locks by default. It saves source content before network work and exact wire bytes before a job POST, preserves uncertain delivery, and processes destinations independently in order. Explicit operator controls retain evidence rather than silently regenerating or deleting an uncertain request. No Entree POS files or live client records were changed.

Validation: **277 .NET tests**, including six Node SDK/TestServer scenarios; **65 SDK tests**, including a real headless Chromium storage check; **21 package checks**. Logs: `%TEMP%\EntreeClientOutboxTests.log`, `%TEMP%\EntreeOutboxSdkTests.log`, `%TEMP%\EntreeOutboxPackageTests.log`. The service scenario uses a flushed file-backed client fixture and new SDK instances around lost ACKs, receipt expiry and ledger reopen; one render and one backend delivery result. The actual IndexedDB test verifies atomic duplicate inserts, stale-update rejection, destination locking across tabs, independence of another printer, and persistence after closing/reopening Chromium with the same synthetic profile. This does not establish sudden power-loss durability, closed-application background delivery, a production Node storage adapter or cross-device coordination.

Package publication now includes the root README and the SDK's required `outbox.mjs` dependency. Prior packages remain unchanged. Installer, deployment, website and release-publication gates are still open.

Fresh package: `dist/EntreePrint-0.0.1-beta-win-x64-client-outbox`, built with intermediates under `%TEMP%\EntreeClientOutboxPublish`. All **30 payload files** passed integrity verification. Both applications report 0.0.1-beta, use .NET 10 / 10.0.12 and retain all eight designed printer-icon frames. Log: `%TEMP%\EntreeClientOutboxPublish.log`. This package was not installed or submitted to GitHub Releases.

## September 14: accepted-job recovery from the POS intent key

The current SDK exposes `getJob({ idempotencyKey })` on both the root API and connected libraries. The authorized `GET /api/jobs/lookup` handler returns the retained original job without any rendering, printer query or submission. Exact Unicode/special-character keys survive encoding, compaction and restart; missing/duplicate/invalid selectors are rejected. Lookup failures remain delivery-uncertain because an earlier request may still be in flight.

All **276 .NET tests and 49 SDK tests pass**. Evidence: `%TEMP%\EntreeJobKeyLookupTests.log`, `%TEMP%\EntreeJobKeySdkTests.log`, and a fresh Node suite run during the connected-library confirmation. Twelve endpoint cases, four SDK cases and the fifth Node/TestServer scenario cover this addition. The integrated scenario saves only a key and service identity to a flushed temporary client checkpoint, loses both print acknowledgements, compacts/reopens service storage and recovers from a new SDK instance. Render/POST counts do not increase and the fake backend is invoked exactly once. This is not an OS process-kill test or a complete durable POS outbox. No physical receipt, listener, installed service or real configuration was changed.

Published `dist/EntreePrint-0.0.1-beta-win-x64-key-lookup` with separate intermediates under `%TEMP%\EntreeJobKeyLookupPublish`. All 28 payload files pass the package manifest verifier; both EXEs/DLLs report 0.0.1-beta, target .NET 10 / 10.0.12 and contain all eight source printer-icon frames. Log: `%TEMP%\EntreeJobKeyLookupPublish.log`. Publication did not install or run the package. The earlier tray preview PID 60072 was independently confirmed still running with Settings open during this check; notification-area placement still awaits operator confirmation.

## September 14: current-runtime tray desktop check and menu recovery

No production tray process was running before inspection. The saved production configuration enables automatic service setup, so the UI check used a separate preview host and temporary settings with setup disabled. Self-contained .NET 10.0.12 preview PID 60072 opened Settings on first launch; the printer title-bar icon and narrow form were visually inspected. Closing Settings left PID 60072 alive with no main window; launching again reopened Settings in that process. The preview is left open for operator verification of the notification icon. This is not an installed-service, logon or production EXE deployment check.

The menu audit also found uncaught settings-read errors in Ports/Diagnostics and out-of-range saved numbers that could prevent Settings from opening. Local menu actions now report expected configuration/filesystem errors without letting them escape the handler, and saved numeric preferences are validated before control construction. Seven new tests verify file preservation, later menu usability, disposed-context rejection and all three numeric bounds. The full suite passes **263 .NET tests**, log `%TEMP%\EntreeTrayMenuRecoveryTests.log`. Desktop evidence paths and the wrapper's scope are documented in samples/TrayPreview/README.md. No real settings, registry startup entry, printer or Windows service was modified.

## September 14: receipt retention and preserved duplicate protection

The full suite passes **256 .NET tests**; the SDK unit suite remains at 45 passing cases from the connected-library change. Logs: `%TEMP%\EntreeRetentionTests.log` and `%TEMP%\EntreeRetentionIntegration.log`. The 23 added tests exercise the current production retention path; no printer settings or installed service were changed.

Finished receipt layouts now compact atomically after a configurable retention period, default seven days from completion. Pending, active and uncertain receipts are excluded. The original command hash, exact request digest, metadata and Windows identity stay in a compact record, so an old request resolves to its original job after restart. No intent IDs are evicted; new work fails explicitly at the count/byte admission limits. Full details and operational limits are in API_V2_IMPLEMENTATION.md.

New checks cover completion-based expiry, pending/uncertain preservation, real-disk compaction and reopen, failed persistence, corruption, concurrent batches, prepared-count and byte-budget recovery, full-ledger duplicate replay, configured policy/expiry API fields, expired original/reprint acknowledgements and the hosted cleanup worker. A fourth actual Node SDK/TestServer scenario prepares with Chromium, simulates delivery, compacts, reopens and proves only one backend delivery. These tests do not simulate actual disk exhaustion or process termination during the atomic replacement, and do not use a physical printer or live service listener.

## September 14: connect returns the print library

The SDK now returns a service-bound print library from explicit and discovered connections. Printer lists are retrieved through its `getPrinters()` method; its `target()` and history/reprint operations retain the original service after another top-level selection. Tests cover late handshakes, retained discovery libraries, concurrent connections, empty inventories, disconnect rejection and metadata isolation. All **45 SDK tests and 233 .NET tests pass**. Three real SDK-to-TestServer scenarios exercise the returned library with the actual renderer, durable job handling and printer-list endpoint. Logs: `%TEMP%\EntreeConnectedLibraryTests.log` and `%TEMP%\EntreeConnectedLibraryDotnet.log`. No physical jobs, listeners or installed-service changes were made for this API update.

## September 14: cashier quality approved

Current source validation: 233 .NET tests pass. Sixteen new network configuration cases cover localhost/IPv6, address preservation, invalid ports and preservation of existing settings after invalid input. The thermal pilot built with zero warnings or errors.

One production-path physical pilot printed through the POS-80C `cashier` queue (USB001), Windows job **23**. Windows supplied 576 dots at 203 DPI / 72.0709 mm printable width. The saved layout contained 80.6979 mm of content and fit one default-driver page. Windows completion flags were 148. The operator confirmed clear output, successful QR/Code128 scans, and satisfaction with clarity. This is the approved short-receipt quality baseline; it is not evidence for another driver, long-receipt feed/cut, network failure or installed-service permissions.

The pilot uses the actual V2 preparation/checksum validation, durable ledger, queue worker, Windows backend and spooler monitor directly, without starting HTTP/UDP listeners. It submitted once and retained the unique intent, exact preview and Windows identity. Reproduction and evidence paths are in samples/ThermalPilot/README.md. Physical approval is recorded separately from machine-generated result JSON.

## Latest source update: shared SDK heartbeat monitoring

`dist/EntreePrint-0.0.1-beta-win-x64-shared-heartbeat` is the newest development package. All 28 payload files, both EXE/DLL versions and eight printer icon frames per EXE passed publication verification. The SDK now passes 42 tests; C# was unchanged from the prior 181-test run and the package build passed. This package has not been installed or launched for tray verification.

Six additional controlled-network cases prove that repeated equivalent configurations reuse a session/handshake, verified same-service aliases share one monitor, credentials/deadlines/heartbeat policies remain isolated, concurrent polls cannot overlap, older handshakes cannot restore stale endpoints, and one address-recovery scan updates multiple prepared-ticket sessions without printing. Connection results are independent copies. Shutdown clears monitor/timer/lifecycle registrations. These tests use fake HTTP/discovery boundaries, real Web Crypto and controlled timers; they do not prove live LAN/OS-resume behavior or cross-node failover. Remaining beta gates still apply.

## Previous source update: code dot alignment

`dist/EntreePrint-0.0.1-beta-win-x64-dot-aligned` is the previous development package. All 28 payload files, both EXE/DLL versions and eight printer icon frames per EXE passed publication verification. That .NET suite passed 181 tests. This package has not been installed or launched for tray verification.

Generated code geometry now preserves encoder dot counts through real Chromium layout; the shared code origin and page boundaries align to driver dots. The exact physical receipt width also constrains the HTML root. Microsoft PDF job 21 completed with flags 388, with all 120 items visible across four inspected pages and both codes decoded from the last page rendered at 300 DPI. The latest proof directory is `output/pdf/long-receipt-dot-aligned`; native-resolution text-band behavior is unchanged. Physical printer quality/scanner/feed/cut and remaining deployment/API gates are still open.

## Previous source update: receipt pagination and barcode width

`dist/EntreePrint-0.0.1-beta-win-x64-pagination` is the previous development package. All 28 payload files passed verification, and both EXE/DLL versions plus all eight icon frames per EXE passed again. That .NET suite passed 173 tests. This package is not installed and its tray has not been launched for UI verification.

The current print path was exercised separately through the named Microsoft Print to PDF queue: job 20 completed as one four-page job with Windows flags 388. Final artifacts are under `output/pdf/long-receipt-pagination-readable-codes`. All four page renders were inspected, covering items 001–120 without missing/repeated lines. Both the Chinese QR value and ORDER10086 barcode decoded from the final page rendered at 300 DPI. The PDF driver rasterized 16 individual text bands at 600 DPI; only 109 item strings are extractable, so full text extraction is not used as a completeness claim. Thermal page feed/cut, scanner and service-account validation remain pending. The earlier thin-barcode PDF experiment under `long-receipt-pagination` is superseded.

## Previous source update: Windows-only status and tray startup

`dist/EntreePrint-0.0.1-beta-win-x64-windows-status` is the previous development package. Its 28 payload files passed publication verification; both EXE/DLL versions and all eight printer icon frames per EXE passed the binary audit again. It removes direct ESC/POS status probing, preserves Unicode Windows query output, and removes the tray's first-launch configuration write/unsaved auto-install. That .NET suite passed 159 tests. The updated Wi-Fi fault-audit sample also compiled with the new inventory constructor.

A read-only `DriverSettingsAudit --inventory` execution returned seven fresh Windows queue observations (one offline). No HTTP/UDP server was started, no direct printer connection was opened by the plugin, and no job was submitted. The tray startup change has build evidence; fresh-install/permission and logon UI validation remain outstanding. This newer directory was not installed or launched and has not been ZIP round-trip tested. The archive below is the earlier build and does not contain these changes.

## Earlier ZIP round-trip audit

Artifact: `dist/EntreePrint-0.0.1-beta-win-x64.zip`, 699,502 bytes.

SHA-256: `9107f1ba48b5aadecefb278abf9ff3e8c63af05e2ae30481781f183e01e98c88`.

This is a development package, not a completed release. See BETA_RELEASE_CHECKLIST.md for unresolved gates.

- Release win-x64 service and tray publication succeeded using separate temporary build intermediates. The publisher refused reuse of an existing output directory in the negative test.
- All 28 payload files passed size/hash/inventory verification, both before archiving and after extracting the ZIP into a new temporary directory. The extracted package's own verification script was used for the second check.
- EXE and DLL versions are 0.0.1-beta / 0.0.1.0. Each published EXE contains all eight icon image resources, matching the designed `assets/entree-print.ico` payloads byte-for-byte. The same check passed after ZIP extraction. The resource audit loads binaries as data; it does not execute either app.
- `powershell -NoProfile -File scripts/test-package.ps1`: 21 checks passed for valid input, same-size content corruption, wrong size, missing/extra/hidden files, missing required SDK entries, empty and duplicate entries (including case variations), traversal/absolute/backslash/stream/trailing-dot paths, malformed hashes/sizes/metadata, directory junctions and publish overwrite prevention.
- `dotnet test EntreePrint.sln --artifacts-path "$env:TEMP\EntreeBetaPackageTests" --verbosity quiet`: 158 passed, none failed or skipped.
- `node --test sdk/test/*.test.mjs`: 36 passed.
- Bundled configuration has an empty access token and no saved jobs or identity files. Runtime prerequisites were read from the published runtimeconfig files: .NET, Windows Desktop and ASP.NET Core 9 for the service; .NET and Windows Desktop 9 for the tray.

No installed service or tray was replaced, launched or stopped by this package audit. No jobs were submitted. This evidence does not prove logon/tray visibility, clean-host install/uninstall, protected network deployment, service-account printer access, physical printing/scanning, or unfinished API/reliability features. Prior automatic approval rejection of the isolated service-launch smoke remains recorded in the release checklist; it was not retried here.
# Service setup failure reporting — September 14, 2026

Latest development package: `dist/EntreePrint-0.0.1-beta-win-x64-service-setup`. Release publication succeeded; all 28 payload files passed integrity verification and both EXEs contain the eight matching printer-icon frames. Both EXE/DLL version pairs are 0.0.1-beta / 0.0.1.0. This package has not been installed or launched. Publish log: `%TEMP%\EntreeServiceSetupPublish.log`.

The service manager checks elevated process completion, reports captured PowerShell errors, waits for Windows service state transitions and cleans temporary scripts in a finally block. Native sc.exe configuration/deletion errors and firewall cmdlet errors stop the operation. Executable paths are single-quoted literals; New-Service receives a quoted binary path. Loopback-only configurations do not create LAN firewall rules, including IPv6 loopback. Status queries now have a real five-second process deadline and distinguish failure from a missing service. The settings-save path catches status-check failures without claiming that runtime settings were applied.

Validation: 191 .NET tests pass using `dotnet test EntreePrintPlugin.Tests/EntreePrintPlugin.Tests.csproj --artifacts-path "$env:TEMP\EntreeServiceSetupTestsBuild" --verbosity quiet`. Ten new child-shell fixtures cover literal Unicode/special-character paths, missing start/restart targets, startup failure, state timeout, native exit failure, firewall failure and three loopback configurations. All service/firewall operations are replaced by test functions; no UAC, Windows configuration changes, server launch or physical jobs occur. Live installation, UAC/error-dialog behavior and logon remain unverified. Firewall cmdlet signatures were checked against Microsoft Learn's New-NetFirewallRule and Remove-NetFirewallRule documentation.
# Responsive tray — September 14, 2026

After the desktop pilot, the Exit menu was also disabled during a service operation so closing the tray cannot interrupt its error reporting/temporary-file cleanup. The three responsiveness tests passed again. Final source was published to `dist/EntreePrint-0.0.1-beta-win-x64-tray-pilot`; its 28-file integrity and both eight-frame EXE icon audits passed. This final package was not launched; the desktop observations below belong to the immediately preceding responsive-tray package. Log: `%TEMP%\EntreeTrayPilotPublish.log`.

Latest development package: `dist/EntreePrint-0.0.1-beta-win-x64-responsive-tray`. All 28 files passed integrity verification; both EXEs contain eight matching printer-icon frames and the expected 0.0.1-beta versions. The full .NET suite passes 194 tests, including three new real WinForms thread cases covering responsiveness while service work is blocked, overlap rejection, subsequent work, disposal with a late worker failure, and malformed configuration preservation. Build/test intermediates: `%TEMP%\EntreeResponsiveTrayTests`; publish log: `%TEMP%\EntreeResponsiveTrayPublish.log`.

Desktop pilot used a new temporary settings file containing only `AutoInstallWindowsService: false`. Before the pilot, process inventory returned no EntreePrintTray process and HKCU Run contained no EntreePrintTray entry. Pilot PID 53016 ran the new package. Its initial process launch used a hidden startup flag; a subsequent normal launch activated Settings, and the narrow layout, controls and title-bar printer icon were visually inspected. Closing Settings left PID 53016 alive with no main window. Launching the EXE normally again opened a new Settings window in that same process; inventory confirmed only one tray instance. The final Close input returned `coordinate input geometry is unavailable`; cleanup stopped only the path-verified pilot process. No Save action, registry update, service command or print job was issued. This verifies repeated-launch activation and current Settings presentation, not a fresh normal first launch, notification-area placement, logon or a real service installation.
# SDK/service interoperability — September 14, 2026

Latest development package: `dist/EntreePrint-0.0.1-beta-win-x64-sdk-integration`. Release publication and all 28 payload integrity checks passed. Both EXE/DLL versions and all eight icon frames per EXE passed the binary audit. Publish log: `%TEMP%\EntreeSdkIntegrationPublish.log`. This package was not installed or launched.

204 .NET tests and 42 Node SDK unit tests pass. Three new integration scenarios execute the actual JavaScript SDK with production request routing, authentication, exact-byte integrity, real Chromium rendering, durable storage and per-printer processing. Stdio connects the SDK's injected fetch boundary to ASP.NET TestServer; printer inventory/driver measurements/delivery are simulated. No network listener or installed service is launched. See `sdk/integration/README.md` for the commands and exact fault/restart scope.

The integration found and fixed a real status-route encoding issue: queue names now use `/api/printers/status?printer={encodedName}&refresh=true`. Seven endpoint cases cover Unicode/slashes, literal percent sequences, other reserved characters, UNC-style names and missing/duplicate query values. The SDK and both API documents use the new single route; no compatibility alias was added. Earlier packages retain the old route and should not be paired with this SDK.
# .NET 10 LTS baseline — September 14, 2026

Latest development package: `dist/EntreePrint-0.0.1-beta-win-x64-net10`. All projects now inherit `net10.0-windows` and minimum runtime patch 10.0.12 from Directory.Build.props. global.json pins SDK 10.0.401 with latest-patch roll-forward. WindowsServices and ASP.NET TestHost package references are 10.0.12. Service/tray runtimeconfig files declare the required base, Windows Desktop and (service only) ASP.NET Core families, all at 10.0.12. The package remains framework-dependent; .NET 9 alone cannot run it.

The isolated x64 SDK at `C:\Users\huang\AppData\Local\EntreePrintBuild\dotnet-10.0.401` was downloaded from Microsoft's builds.dotnet.microsoft.com release URL and verified against the release-metadata SHA-512 value `24b670ad3d923bfcf47df6c3b034152398b42f6dbc388e10d783aee1cfb5e5817d399fc0ae2a12cfa822a55e61d34830ccb15c50ef6efee437ab874bb7c79430` before extraction/execution. Its inventory reports SDK 10.0.401 and all three runtime families at 10.0.12. Build shells set DOTNET_ROOT to that directory and prepend it to PATH; no machine-wide runtime or permanent PATH change was made.

Validation: all 204 .NET tests passed on .NET 10.0.12, including actual SDK-to-handler/Chromium/durable-ledger and WinForms message-loop checks. All six sample projects built with zero warnings/errors, without running the samples. A forced uncached restore with NuGetAudit=true, NuGetAuditMode=all, NuGetAuditLevel=low and warnings-as-errors completed without known vulnerability or audit warnings. Logs: `%TEMP%\EntreeNet10Tests.log`, `%TEMP%\EntreeNet10Audit.log`, `%TEMP%\EntreeNet10Publish.log`.

Release publication passed 28-file integrity, expected EXE/DLL versions, eight icon frames in each EXE, and the new runtime-family/patch-baseline audit. Three mutated copies were rejected for old patch, missing Desktop runtime family and wrong target framework. Fixtures remain at `%TEMP%\EntreeRuntimeAudit-6512afe35d954e99a777686250c5e13e`. The existing 21 synthetic package checks also passed. The new package was not installed or launched; previous .NET 9 desktop and virtual-printer pilots do not prove .NET 10 deployment or hardware behavior. Microsoft support policy and download metadata were refreshed September 14, 2026; setup documentation points to the official runtime downloads and support/OS policies.
# Windows handoff identity recovery — September 14, 2026

Latest development package: `dist/EntreePrint-0.0.1-beta-win-x64-spooler-identity`. Publication passed all 28 payload integrity checks, both EXE/DLL version checks, the .NET 10.0.12 runtime audit and all eight icon frames per EXE. Log: `%TEMP%\EntreeSpoolerIdentityPublish.log`. This package was not installed or launched.

Current .NET suite: 217 passing tests on .NET 10.0.12; SDK unit suite remains 42. Thirteen new cases cover recovering a missing Windows ID by persisted document name, exact queue/document matching, ambiguous/missing candidates, stale/reused IDs, atomic persistence failure, live callback ordering, blocking active partial reprints, worker-failure ordering and continued monitoring beyond one day. Log: `%TEMP%\EntreeSpoolerIdentityFullTests.log`.

The native `samples/SpoolerIdentityAudit` created virtual Windows job 22 on the explicitly named Microsoft Print to PDF queue. Before drawing, it deliberately omitted saving the numeric ID, closed and reopened a temporary ledger, enumerated Windows jobs and matched the unique persisted document identity. The result was `needs_attention` with `spoolerState=submitted`, `handoffUncertain=true` and `evidence=queue_identity`; explicit reprint and automatic retry were blocked. The callback then aborted before any page or EndDoc. The same ID/document pair was verified absent after AbortDoc, and no PDF file was created. No thermal queue, service or network listener was used. Other jobs were untouched; the virtual queue initially contained zero jobs.

Evidence: `%TEMP%\EntreeSpoolerIdentityAudit\34de6a1968374020bf4f3e312b1a0ff8\result.json` and its retained `jobs` ledger; log `%TEMP%\EntreeSpoolerIdentityAudit.log`. The final source additionally preserves early active evidence when a worker reports partial failure, covered by the full test run. The native audit is not an actual process-kill/service-restart or complete-output test. Microsoft EnumJobs/JOB_INFO_1 documentation was checked for native structure and buffer semantics. Queue enumeration is limited to 8 MB/8,192 jobs and incomplete snapshots are rejected; a failed identity scan does not stop known-ID monitoring.
