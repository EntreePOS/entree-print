# 0.0.1-beta release evidence

Hosted HTTPS run [34834324901](https://github.com/EntreePOS/entree-print/actions/runs/34834324901) completed with **385 passed and one failed .NET test**, before building an installer. All HTTPS cases passed. The existing system-command discovery fixture hit its test-only 20-second timeout; it now uses the application's bounded 90-second deadline, while the dedicated one-second timeout test stays unchanged. A new hosted run must pass before this source is treated as a verified installer candidate.

HTTPS progress: optional Windows certificate-store HTTPS now replaces HTTP on the single configured port, with certificate/host validation and no startup downgrade. Native discovery carries the protocol, HTTPS recovery rejects HTTP candidates, and SDK monitors are isolated by protocol. The playground checks localhost HTTP/HTTPS and falls back to editable preview mode. Local verification passes **386 .NET tests, 98 core SDK tests and nine playground tests**, with eleven guide snippets verified. TLS success, wrong-host and untrusted-issuer rejection use real Kestrel over memory streams; no certificate store, production settings or print listener was changed. Installed service private-key access, client trust, renewal and live tablet/LAN tests remain open. See [HTTPS setup](BETA_SETUP.md#https-setup). A new installer candidate is required for these changes; the verified candidate below predates them.

Latest verified candidate: [run 34831638120](https://github.com/EntreePOS/entree-print/actions/runs/34831638120), source `02110129a19bd07b4ef97ee5de43abeb4d601542`, passed **374 .NET, 95 core SDK and nine native SQLite tests**, plus **64 hosted installer checkpoints** covering 739 installed entries. Downloaded installer SHA-256 and five successful native lifecycle logs were verified. This includes ordered receipt actions and the Chromium startup-file fix, superseding the failures and older candidates below. See PACKAGE_VERIFICATION.md and `installer/evidence/windows-ci-34831638120.json`. Interactive deployment, tray/logon, service/network recovery and remaining API/transport requirements remain open; no release is published.

Current host check: cashier, kitchen and bar all use POS-80C on USB001. The old prototype service is still running as PID 6252 from its unquoted development path and was not changed. A separate Windows/network-printer pilot environment has been requested; availability is not yet confirmed.

Renderer startup correction: [run 34831155099](https://github.com/EntreePOS/entree-print/actions/runs/34831155099) passed the corrected storage test but exposed the renderer's exact failure: Windows sharing violation while Chromium wrote `DevToolsActivePort`. The engine now waits through missing/incomplete startup data and only transient sharing/lock violations, within the existing render deadline. Browser exit, cancellation and permission errors still stop preparation. Four new real-file cases cover this boundary; all **374 .NET tests** pass locally. The first missing/incomplete fixture briefly raced its own writer and was corrected to publish between polling reads. A rerun is required before claiming an updated installer. The intermediate run 34831048924 selected the older commit and was cancelled without building.

Candidate run [34830630831](https://github.com/EntreePOS/entree-print/actions/runs/34830630831) stopped at tests (368 passed, two failed), before building an installer. The new storage-barrier fixture used the production five-second retry delay against a five-second assertion deadline; it now sets a 250 ms fixture delay and waits for the actual storage-blocked notification instead of a fixed sleep. The separate SDK offline scenario returned a renderer error whose server exception was not included in its assertion; failed scenarios now include captured server errors. Its cause is not yet established. The full local suite passes all 370 tests after these test-only corrections; hosted verification must be rerun.

Ordered actions: the source now supports receipt → optional cut → optional beep under one intent, with per-phase Windows evidence in the atomic job record. Restart never repeats completed receipt work or uncertain actions; phase-storage failures keep later plugin receipts from overtaking unresolved work. All **370 .NET and 95 core SDK tests** pass locally, including nineteen new action cases and the ninth SDK/TestServer scenario. Raw cutting requires explicit `CutMode: "raw"`; driver mode is the default. These changes are newer than the installer candidate below. Physical beep/cut, live service/LAN recovery and the remaining release gates are still open.

Latest candidate: [run 34827971685](https://github.com/EntreePOS/entree-print/actions/runs/34827971685) passed from `d2fd553335b71bb613098d20bd888e0d9ba5c904`, including durable status storage and SDK/SSE recovery. Downloaded evidence confirms **350 .NET, 93 core SDK and nine native SQLite tests**, plus **64 installer lifecycle checkpoints** over 739 installed entries. The downloaded EXE SHA-256 matches the recorded result and metadata. See `installer/evidence/windows-ci-34827971685.json` and PACKAGE_VERIFICATION.md. Interactive installation/logon, live service/LAN recovery, ordered actions and remaining release gates are still open; no GitHub Release is published.

Resumable status updates: authenticated SSE, service-owned resume cursors, captured public job versions, printer removal events and locked printer checkpoints are now implemented. The SDK shares streams, validates sequences/ownership, refreshes snapshots after pruned history and merges job versions without submitting work. **350 .NET tests, 93 core SDK tests, eight playground tests, ten guide snippets and 22 package checks pass locally.** This includes 15 SSE/API cases and an eighth real SDK/TestServer scenario that replays across ledger reopen with zero renders/submissions/deliveries. The storage fault test was corrected to accept Windows' `UnauthorizedAccessException` for a directory occupying a file path. The candidate above now includes these changes; live LAN/service interruption gates remain open. This supersedes the earlier SSE/SDK-pending status below.

Durable event foundation: service-side replay storage is now bounded to 2,000 events/16 MB, with stable sequence IDs and explicit cursor-gap errors. Atomic job records retain up to 256 pending version snapshots so event-storage failures do not erase status transitions or falsely reject accepted jobs. Restart, later writes and periodic maintenance retry publication. All 334 .NET tests pass, including fourteen new storage/recovery cases; API/SDK integration now runs with the durable broadcaster. Public SSE, snapshot reconciliation and SDK job/printer subscriptions remain required; this does not close the event gate. A transient renderer 503 during one full-suite run was not reproduced in the isolated rerun or subsequent full run; enhanced test-side exception capture is retained for investigation.

Latest packaged verification: [run 34823530375](https://github.com/EntreePOS/entree-print/actions/runs/34823530375) passed the SQLite outbox candidate from `bc96eef5a8cd208df178a4d67a0a0b27d1436ae7`: **320 .NET, 81 core SDK and nine native SQLite tests**, plus all **64 installer lifecycle checkpoints**. Installed permissions cover 738 entries. Downloaded TRX, native logs and installer digest were verified; `installer/evidence/windows-ci-34823530375.json` retains the exact result. Interactive Windows, live service and physical/network recovery gates remain open.

Native client outbox: an optional SQLite adapter now retains source and exact request bytes for Node/Electron main clients. Short committed transactions are independent from per-destination OS-backed SQLite locks, so an exited process releases its dispatch lock without rolling back already saved requests. Nine native cases cover process exit, two-process deduplication, concurrent SDK recovery, queue independence, stale writes, capacity and corruption. A seventh real SDK/TestServer scenario uses this adapter across client/store replacement and expired service layouts with one render/delivery. All **320 .NET tests, 81 core SDK tests, nine native SQLite tests and 22 package checks** pass locally. This covers local process recovery, not machine power loss, network filesystems or cross-device ownership. Local pruning/export and live Windows/network recovery remain open.

Latest packaged verification: [run 34822273890](https://github.com/EntreePOS/entree-print/actions/runs/34822273890) passed from `297a4645b5018f1469886bb30c983ccbbd8dec4b`, including the bounded settings reader. All **319 .NET tests and 64 installer lifecycle checkpoints** passed. Downloaded installer hash and all five native success logs were verified; exact result is retained in `installer/evidence/windows-ci-34822273890.json`. This supersedes the older candidate below for current code coverage, with the same limits on interactive UAC/logon and live printing evidence.

Settings-request correction: the elevated helper now reads one open request handle without write/delete sharing, caps actual input at 1 MB and rejects missing, null, malformed or invalid settings before saving. Oversize requests are also rejected before requesting elevation. Eleven new cases cover Unicode/service-only fields, exact byte boundaries, a concurrent writer, growth after size observation and unchanged existing configuration; all **319 .NET tests** pass. This is automated request-boundary evidence, not an interactive UAC pilot.

Hosted installer gate: [run 34819567689](https://github.com/EntreePOS/entree-print/actions/runs/34819567689) passed real silent installation, same-version update, uninstall and reinstall on a fresh Windows runner. All 308 .NET tests and 64 recorded lifecycle checkpoints passed, including permissions across 737 installed entries, fixed settings save, byte retention, shortcuts and loaded-user startup cleanup. The unsigned tested installer and logs are a development workflow artifact. This closes the hosted installer/storage portion of the gate; interactive UAC, a client without installed .NET, actual logon, live service startup and accepted-job/hardware recovery remain open. See PACKAGE_VERIFICATION.md and the retained installer/evidence result.

Service-management progress: the tray now elevates a fixed action in its installed EXE, constructs commands after elevation, and uses system PowerShell without temporary script/error files. All five actions verify the exact quoted service executable before service or firewall changes; foreign, unquoted and extra-argument commands are rejected. Saved configuration and executable paths must have trusted permissions. System-only command/module lookup and a 90-second operation deadline are enforced. All **308 .NET tests** pass, including ownership rejection, lookup failure, literal Unicode paths, successful actions, missing services and timeout uncertainty. The existing prototype remains running as PID 6252; no live service or firewall was changed. Clean-host installation, UAC, runtime dependency permissions and hardware recovery pilots remain open.

Protected-settings progress: installation now initializes administrator-owned data directories; installed configuration writes use a fixed elevated tray entry point with a bounded data-only JSON request and fixed output location. Settings and Ports save asynchronously. Windows service startup rejects unsafe configuration/storage permissions and links before claiming the ledger or opening listeners, and browser/service executable checks reject user-writable paths. Existing unsafe folders are not modified or adopted. All **291 .NET tests** pass, including 13 new permission-policy, unchanged-data and isolated-save cases; existing real Chromium preparation checks pass with the installed browser's ACLs. Clean-host ACL creation, UAC with another administrator account, and runtime dependency permissions are still release gates. The temporary service-management script path was replaced in the later service-management change above. No protected production directory or running service was changed during these tests.

Installer progress: a self-contained Windows x64 setup EXE now builds with pinned Inno Setup 7.1.0. The input contains 701 verified files, .NET 10.0.12, and both designed EXE icons. Default receipt storage moved outside the application folder to ProgramData; explicit paths remain supported. All **278 .NET tests**, **22 package checks** and **nine isolated installer lifecycle checks** pass. A real read-only check rejected a different running prototype installation and confirmed its process remained running. The setup EXE is unsigned and has not been installed or uninstalled on a clean host. Service-account/configuration ACLs, tray logon/startup cleanup and real deployment/recovery pilots remain release gates. See `installer/README.md`; compilation is not release approval.

Playground progress: the public usage site now includes editable receipt/kitchen/code examples, browser drafts, optional SDK connection and service rendering, generated connected-library API code and rendered-HTML download. The page never submits a print job. Desktop/mobile and validation/copy/theme checks passed; browser testing also found and fixed default timer receiver binding in the SDK. All **66 SDK tests** pass. Actual browser-to-installed-service rendering remains a live pilot.

Updated product scope: Entree Print is a standalone Windows printing product; Entree POS is one client. Source and the root README are published at [EntreePOS/entree-print](https://github.com/EntreePOS/entree-print), and the [usage guide](https://entreepos.github.io/entree-print/) is live through GitHub Pages. The guide follows entree-xlsx's example-led documentation approach. A Windows installer and installer asset on the GitHub release remain required once 0.0.1 is ready. Migration of Entree POS's business flows is a separate client project, not a prerequisite for the standalone product release; representative client integration checks remain required.

Publication check: initial standalone source commit `b53158d4ac3df339a9f6bdfbc02e3ec4633d2c7d` contains 185 source/test/documentation files. GitHub Pages workflow run `34813602033` completed successfully. The live page loads the printer icon and saved receipt, and its nine snippets and local links pass verification. Desktop and 320/375 px mobile layouts were inspected in light/dark themes; topic search, no-result feedback and code copy were checked. A clean archive of the published source passes all 277 .NET tests. This publishes development documentation, not an installer or mature release.

Client outbox implementation: the optional SDK outbox persists unsent source content, then the exact prepared request before submission. A real Chromium check verifies IndexedDB commit/reopen and cross-tab destination locking. A sixth actual SDK/service scenario uses a flushed file fixture, new SDK instances, lost acknowledgements, expired layout and reopened service ledger; one render and one backend delivery result. Storage failure, per-destination independence, immutable keys, explicit retry/cancel and retained uncertainty have SDK coverage. The default store is browser/Electron-renderer only; automatic local pruning and a production Node store remain unfinished. The current full .NET suite passes **277 tests**.

Latest recovery check: `print.getJob({ idempotencyKey })` now retrieves an accepted job from the owning service using the POS's persisted key, without rendering or submitting. Lookup survives layout compaction and ledger reopen. Twelve endpoint cases, four SDK cases and a fifth actual SDK/service scenario verify exact keys, authorization, service binding, validation and recovery after both acknowledgements are lost. **276 .NET tests and 49 SDK tests pass**. The integrated client checkpoint stores only the intent key and service identity; a full POS outbox for unsent work and cross-node ownership are still pending.

Latest tray check: a self-contained .NET 10.0.12 host of the production tray context opened Settings visibly on first launch, displayed the printer title-bar icon and narrow layout, stayed running after Settings closed, and reopened Settings in the same process on a second launch. The isolated preview remains available for operator confirmation of notification-area placement. This verifies the current context/runtime UI, not installed EXE deployment or logon. Seven new regression cases protect Ports/Diagnostics/menu errors and malformed numeric settings; **263 .NET tests pass**. See samples/TrayPreview/README.md for scope and evidence.

Retention implementation: finished receipt layouts now compact after a configurable 1–365 days (default seven), while pending and delivery-uncertain receipts remain intact. Compact intent records retain exact-byte replay protection and Windows evidence across restart. Admission is bounded to 100,000 intents, 10,000 prepared receipts and a 512 MB ledger byte budget; keys are never evicted to make room. Layout expiry is exposed to POS, and capacity rejects new work without dropping accepted jobs. All **256 .NET tests pass**, including 23 new storage/endpoint/hosted-worker cases and a fourth actual SDK/service scenario covering expiry, atomic failure, corrupted records, quota recovery, restart and no duplicate delivery. Long-term export/removal, actual disk-full and process-kill-during-compaction pilots remain separate checks.

Latest API correction: all SDK connect variants now return a connected print library. Printer objects are retrieved with `await print.getPrinters()` and receipt calls use `print.target(name)`. Each returned library keeps its service binding even after another top-level connection. Server metadata is available through `print.connection`; no printer list is exposed on the library itself. All 45 SDK tests and 233 .NET tests pass, including the actual service-handler integration scenarios. No additional physical receipt was printed for this SDK change.

Latest physical pilot (September 14): after the operator confirmed `cashier` ready, one production-path test reached Windows job **23** and reported completion (flags 148). POS-80C / USB001 reports 576 printable dots at 203 DPI, so the API selected 72.0709 mm automatically. The Chinese/English/QR/Code128 receipt fits one page with 80.6979 mm content height. The operator confirmed **clear printing and successful scans of both codes**, then explicitly approved clarity. This passes the short-receipt quality baseline for this printer/driver. Long receipts, network failures and service-account deployment remain separate gates. See samples/ThermalPilot/README.md.

Network configuration correction: `localhost` is normalized to IPv4 loopback; IPv6 listener/diagnostic URLs are bracketed correctly; local diagnostics follow the configured address. Saving Basic preferences preserves an explicit bind IP unless network access is toggled. Invalid addresses/ports fail clearly before overwriting saved configuration. Regression tests cover service/tray behavior and malformed persisted addresses.

The requested beta is not yet release-ready. This checklist tracks the agreed API and reliability requirements, rather than treating the existing prototype as a finished release. Latest scope decision: first release 0.0.1 with no backward compatibility. Use one `/api` surface and one `AccessToken`; remove prototype compatibility requirements.

| Requirement | Evidence required | Current status |
| --- | --- | --- |
| Standalone product distribution | `EntreePOS/entree-print` source, README, usage site, Windows installer, verified installation/removal and release asset | Repository, README, usage guide and playground are published. A self-contained installer builds and passes silent hosted installation/update/uninstall/reinstall. Candidate details are above. Interactive client deployment and the mature GitHub release asset remain pending |
| Tray discoverability and printer EXE icon | Manual launch opens settings, repeated launch activates existing UI, background launch maintains notification icon, packaged EXEs have multi-size icons | Fresh service/tray EXE resource audit confirms all eight designed icon frames byte-for-byte and both EXE/DLL versions 0.0.1-beta. Earlier published tray Settings/repeated-launch desktop check passed; current simplified UI and logon/overflow verification remain |
| Packaging and installation | Versioned reproducible package, integrity verification, service install/start, per-user tray startup, documented prerequisites/uninstall | Self-contained .NET 10.0.12 win-x64 candidate with designed EXE icons, SDK and documentation. Local verification passes 22 package and nine isolated installer cases; hosted lifecycle evidence covers 64 checkpoints and installed permissions. Interactive UAC/alternate administrator, no-runtime client, service activation, fresh logon and accepted-job recovery remain open |
| Fluent API and HTML preview | Executable config/connect/search/target/render/print examples and tests | Connected-library API, immutable tickets, exact-byte retry, ordered receipt actions and resumable job/printer events are implemented. All 98 core SDK tests and nine SDK/TestServer scenarios pass, including real Chromium preparation and ledger reopen. Nine playground tests and eleven guide snippets pass. Live network/Windows integration remains open; see API_V2_IMPLEMENTATION.md and sdk/integration/README.md |
| D quality through Windows spooler | Production text/vector renderer, preview equivalence, tracked Windows job ID, physical quality fixture | Shared D drawing and one tracked GDI job now support multiple default-driver pages. PDF job 20 completed with 120 items across four inspected pages. Some driver-rasterized text bands remain at full 600 DPI; continuous HTML preview does not show page boundaries. Cashier job 23 passed operator quality approval and QR/Code128 scanning; long-receipt feed/cut checks remain |
| Chinese, QR and barcodes, no image API | Unicode/layout fixtures and physical scanner validation | Eight 203/300/600-DPI code fixtures plus four page-relocation cases decode. QR/Code128 also decode from the final PDF page rendered at 300 DPI. Physical barcode bar width now scales with DPI; codes/labels/quiet zones stay together across pages. Driver DPI/width are read automatically and image API blocks are rejected. Code-origin and page-break dot alignment now pass six real Chromium cases at 203/300/600 DPI; cashier QR/Code128 scanner validation passed; other printers remain unverified |
| Validated durable jobs and deduplication | Byte-integrity/schema checks, atomic key/payload storage, restart recovery, concurrent duplicate tests | Exact UTF-8 byte digest, bounded reads, duplicate-key rejection, schema fields, auth and service-identity guards implemented. V2 endpoint tests cover invalid packets, concurrent duplicates, changed payloads, expired new previews and lost-ACK replay. Accepted layouts retained in the atomic ledger and integrity checked on restart. Six new real-disk/fake-printer tests verify waiting without expiry for observed offline/paper/cover/pause conditions, restart ordering, online resumption and no duplicate completed destination; artifact retention and retained deduplication records now implemented; long-term history export remains |
| Spooler evidence and safe recovery | Job-specific progress/completion, unknown outcome without automatic replay, offline/paper-out/restart tests | Queue notifications, GetJob and bounded EnumJobs identity recovery implemented. Native Microsoft PDF job 22 was matched after the ID was omitted and its ledger reopened; it stayed uncertain, blocked reprint while active, and was aborted/removed before any page. Thirteen new cases cover identity/queue ambiguity, persistence failure, active partial jobs, callback races and monitoring beyond one day. Actual process-kill, Windows service/spooler restart and hardware pilots remain pending |
| Discovery and heartbeat | First-found initial connection, same-service reconnect, stale printer status, timeout/resume tests | Native IPv4 discovery, first-success connection, heartbeat and identity-constrained address recovery have controlled coverage. Authenticated SSE resumes durable events and refreshes snapshots after history expiry, with SDK/TestServer reopen coverage. Real LAN/OS resume remains unverified; the earlier isolated HTTP/UDP runtime check was rejected by automatic approval review and was not retried |
| Standalone client integration readiness | Bundled SDK examples, no false printed flags, durable ownership independent of business audit | Connected-library API, persisted-key lookup and optional durable client outbox implemented for browsers and native SQLite clients. Real browser storage, native process-exit and SDK/service checks cover reload/retry behavior. Local outbox pruning and client-owned routing/UI remain distinct work. Entree POS migration is separately tracked and does not define the standalone release |
| Configured node failover | New work continues through another node to the same Ethernet printer; uncertain/accepted jobs retain ownership; USB cashier stays pinned | Ordered initial `connect({ nodes })` selection implemented with bounded deadlines, explicit credentials/identities and backup network-queue restrictions. Fifteen new simulated SDK cases cover selection/cancellation, owner isolation, USB/device/reprint restrictions and saved outbox wire. All 81 SDK tests, eight playground tests and six existing SDK/service integration scenarios pass. Automatic routing during printing, verified physical destination equivalence, durable cross-node ownership, portable previews and live multi-node faults remain pending |
| Settings and API access control | Driver-owned printer preferences, saved service settings, API credential and protected transport | Driver measurements supply width/DPI and printing preserves driver settings; no duplicate printer editor. Protected atomic saves and optional certificate-store HTTPS are implemented. HTTPS invalid-configuration and TLS rejection tests pass. Installed service certificate/private-key access, client trust, service-account driver defaults and physical feed/cut remain open |
| Beta verification | Build/tests, clean-package smoke run, release notes with verified limits, USB/Ethernet/Wi-Fi pilot evidence | Current local checks pass 386 .NET, 98 core SDK and nine native SQLite tests. Hosted installer evidence and the cashier quality/scanner pilot are recorded above. Earlier PDF, process-kill and simulated Wi-Fi evidence retains its stated scope. Interactive package deployment, physical ordered actions, live service/spooler restart and network failure pilots remain open; no release tag or installer release asset yet |

Do not mark the release complete from a successful build or package hash check alone. References: API_DESIGN.md, API_V2_IMPLEMENTATION.md, sdk/integration/README.md and the sample verification guides.

Windows handoff identity gate: persisted attempts with no numeric Windows ID now remain monitored and use bounded queue enumeration to find exactly one matching document name in the original queue. No match/ambiguous match never permits automatic resubmission. Active Windows evidence prevents a reprint even when full receipt handoff is uncertain; completion flags alone cannot complete that partial receipt. Monitoring no longer silently ends after one day. Native virtual job 22 exercised the StartDoc-to-ID-save gap and was removed by AbortDoc before drawing. See samples/SpoolerIdentityAudit/README.md and PACKAGE_VERIFICATION.md for exact scope and evidence.

Runtime support gate: all projects now inherit `net10.0-windows` and runtime baseline 10.0.12, with SDK 10.0.401 pinned. All 204 .NET tests pass on the new runtime and all six sample projects build without warnings. A fresh all-transitive NuGet audit with warnings treated as errors reported no known vulnerabilities. Publication now also checks each EXE's runtime configuration for the correct framework families and patch baseline. Microsoft's September 8, 2026 support table lists .NET 10 LTS through November 14, 2028. Clean-host installation must supply x64 .NET, Desktop and ASP.NET Core 10.0.12 or a later 10.0 patch; earlier packages and historical virtual-printer/UI pilots used .NET 9 and do not prove current-runtime deployment.

SDK/service interoperability gate: three Node SDK scenarios now execute against the actual API handlers, renderer, ledger and processor using a stdio bridge to TestServer. Lost ACKs and invalid ACK digests retain the original intent; corrupted requests create no job; expired-preview replay after a store reopen returns the accepted job. One completed destination is not replayed when another waits offline across restart. Seven additional status-handler cases protect exact queue names and reject missing/duplicate names. Status uses a query parameter because encoded slashes in route segments were not preserved. No listener or installed service was launched and no Windows/physical jobs were submitted.

Service setup failure gate: the tray now checks the elevated script exit code and displays the Windows error, waits for the requested service state, and cleans up temporary scripts after failure or cancellation. Native service configuration failures and firewall failures stop setup. Executable and diagnostic paths remain literal, including Chinese text, quotes and dollar expressions. Ten new ordinary child-shell tests replace all service/firewall operations; the full .NET suite passes 191 tests. This does not verify UAC, a real install/uninstall, service-account access or fresh-logon tray behavior. The standalone install/uninstall scripts use the same failure checks.

Tray responsiveness gate: service operations and status checks now run away from the UI thread, with one operation at a time and completion/error UI suppressed after disposal. Malformed/unreadable configuration leaves the icon available and skips automatic setup. Three real Windows Forms thread tests cover message-loop progress during blocked work, duplicate-operation rejection, later-operation acceptance, disposal with a late failure, and preservation of malformed settings. The full .NET suite passes 194 tests. A temporary-config run of the new package verified the narrow settings window, title-bar printer icon, close-to-background behavior and repeated-launch activation in the same process. No tray process or tray logon registry entry existed before this pilot. Notification-area/overflow placement, fresh logon and installed-service behavior remain unverified. The temporary pilot was closed without saving or changing startup/service settings.

Dot-alignment software gate: code encoder dimensions survive Chromium extraction, origin/edges are snapped to printer dots, and page breaks retain alignment. Six real Chromium fixtures cover fractional offsets and left/center/right layouts at 203/300/600 DPI, alongside unchanged-text and DPI-mismatch checks. Latest PDF job 21 completed with 120 items across four visually checked pages; both codes decoded from a 300-DPI final-page render. Thermal quality/scanner and feed/cut pilots remain open.

Latest driver-ownership correction supersedes earlier manual printer-profile requirements. Long receipts now paginate within driver default pages without shrinking lines or splitting codes/keep-together sections. Oversize indivisible sections fail before StartDoc. Current-path PDF job 20 completed as one four-page Windows job (flags 388); all 120 items were visually checked across the rendered pages, and both QR/Code128 decoded from a 300-DPI rendering. The 600-DPI driver rasterized some individual text bands at native resolution; a fully vector/searchable PDF is not claimed. Thermal page feed/cutting, service-account settings and physical scanners still require verification. See samples/BetaSmoke/README.md for reproducible evidence.

Latest status/startup changes: removed the unsolicited ESC/POS network probe and its configuration. Windows inventory now uses explicit UTF-8, covered by two real child-process fixtures. A read-only audit found seven installed queues (one Windows-reported offline), with no receipt/device jobs or direct printer connections. The tray no longer writes missing configuration before creating its icon or auto-installs the service before the first settings save. The newer `dist/EntreePrint-0.0.1-beta-win-x64-windows-status` package passed 28-file integrity and binary version/icon verification. Fresh-install/permissions and logon UI verification remain required. Current .NET suite: 181 passing tests. See PACKAGE_VERIFICATION.md for artifact scope.
